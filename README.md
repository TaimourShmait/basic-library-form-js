# Basic Library Form JS

A simple book manager using JavaScript ES6 classes and DOM manipulation to add and display books in a table.

## Installation
1. Clone this repository:  
   `git clone https://github.com/TaimourShmait/basic-library-form-js.git`  
2. Open the `index.html` file in your browser.
